import { Context, MiddlewareFn } from 'telegraf';
import { SessionKeyFunction } from './keys';
export declare type SessionOptions = {
    sessionName: string;
    collectionName: string;
    sessionKeyFn: SessionKeyFunction;
};
export declare const session: <C extends Context<import("typegram").Update> = Context<import("typegram").Update>>(sessionOptions?: Partial<SessionOptions>) => MiddlewareFn<C>;
